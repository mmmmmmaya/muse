import unittest

from model import connect_to_db, db, Recording


class Test(unittest.TestCase):
    """Hold off on writing these tests until I decide if I'm going to
    change the table schema for KeyPresses."""

    pass

if __name__ == '__main__':
    unittest.main()
